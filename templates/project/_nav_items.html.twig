{% for item in items %}
<li class="nav-item">
    <div class="row">
        <div class="col-6">
            <a class="nav-link" href="{{ path('app_project_show', {projectUuid: item.uuid}) }}">
                {{ item.name }}
            </a>
        </div>
        <div class="col-6 text-end">
            <a class="nav-link" href="{{ path('app_project_edit', {projectUuid: item.uuid}) }}">
                edit
            </a>
        </div>
    </div>

</li>
{% endfor %}