<div class="row">

    {{ form_start(form) }}
    <div class="col-6">
        {{ form_row(form.name) }}
    </div>
    <div class="col-6">
    </div>

    <div class="col-12">
        {{ form_row(form.content) }}
    </div>

    <div class="col-12">
        <hr>
        {{ include('elements/form_buttons.html.twig', {back_uri: path('app_project_edit', {projectUuid: project.uuid})}) }}

    </div>


    {{ form_end(form) }}

</div>