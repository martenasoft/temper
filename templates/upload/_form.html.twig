<div class="row">
    <div class="col-6">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ include('elements/form_buttons.html.twig') }}

        {{ form_end(form) }}
    </div>
</div>
